import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plantilla.camaraShareApp',
  appName: 'IACOBRANZA',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
